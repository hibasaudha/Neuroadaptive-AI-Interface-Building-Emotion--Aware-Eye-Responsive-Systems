# CORE LIBRARIES
# ----------------------------------------------------------------------
# Deep Learning framework for the FER model
tensorflow==2.14.0
# Or if using PyTorch: torch==2.1.0

# Computer Vision libraries
opencv-python==4.8.1.78
# High-performance facial landmark detection (dependency for EAR)
dlib==19.24.2

# Backend Web Server (Choose one: Flask is common for simple projects, FastAPI for high performance)
Flask==2.3.3
# Or: fastapi==0.104.1
# If using Flask, also need:
waitress==3.0.0
gunicorn==21.2.0

# GOOGLE GENERATIVE AI
# ----------------------------------------------------------------------
# SDK for connecting to the Gemini API
google-genai==0.1.0 # Or the latest stable version

# DATA PROCESSING AND UTILITIES
# ----------------------------------------------------------------------
# NumPy for efficient numerical operations (used heavily by CV)
numpy==1.26.2
# Pandas (optional, but good for handling audit logs/data analysis)
pandas==2.1.4
# Scikit-learn (useful for model evaluation metrics: confusion matrix, accuracy)
scikit-learn==1.3.2

# HELPER UTILITIES
# ----------------------------------------------------------------------
# Used for accessing environment variables securely (like the API key)
python-dotenv==1.0.0